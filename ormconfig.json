{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "twitter-archive",
  "synchronize": true,
  "logging": true,
  "entities": ["src/entities/**/*.ts"],
  "cli": {
    "entitiesDir": "src/entities",
    "migrationsDir": "src/migrations"
  }
}
